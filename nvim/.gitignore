tags
test.sh
.luarc.json
nvim

spell/

# lazy-lock.json tracked here (upstream kickstart ignores it to avoid fork merge
# conflicts) so fresh installs get pinned plugin versions.
# https://lazy.folke.io/usage/lockfile

.DS_Store
